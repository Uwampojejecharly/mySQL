# How to list all employees alphabetically:
# In alphabetical order by default:
SELECT * FROM employees
ORDER BY last_name;

# In descending order:
SELECT * FROM employees
ORDER BY last_name DESC;

# Sort by amount:
SELECT * FROM transactions
ORDER BY amount;
